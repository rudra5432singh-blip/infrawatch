version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: infrawatch-backend
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
    volumes:
      - ./backend/data:/app/data
      - ./backend/ml/models:/app/ml/models
    command: >
      sh -c "python data/seed.py && python ml/train.py && uvicorn main:app --host 0.0.0.0 --port 8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: infrawatch-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: unless-stopped
